# tokens/

lightningcss source for the ODC theme. `index.css` imports `colors.css`, `spacing.css`, `typography.css`.
`npm run build:theme` bundles them to a single flat `dist/theme.css` to paste into the ODC Theme editor.
These values ARE the brand — build to them exactly; contrast failures are findings, not edits here.
