# Findings Register — World Bank Group / WBG
> Next ID: FND-070
>
> Local mirror of design-conformance findings. Live tracker = GitHub Issues labeled `finding`.
> The implementation always matches the approved design; rows here track conflicts awaiting a design/brand decision.
>
> Status: all 10 open findings FILED as GitHub Bug issues **#12–#21** (2026-06-17), assigned to
> @kharloridado for designer confirmation. FND-002 resolved pre-filing (Open Sans). Payloads
> mirror in `findings/tickets/`. (A duplicate set #1–#11 from a re-run was closed as not-planned.)

| ID | Type | Sev | Location | Observed | Rule | Recommendation | Disposition | Issue |
|---|---|---|---|---|---|---|---|---|
| FND-001 | consistency | high | Type/role page (node:10995-7259) | Foreign token systems leaked into the WBG library: `_ITSES` (Open Sans), `_GDS` (Manrope), `Headings Desktop` (Andes), `DS L2.0` (IBM Plex Sans) + strays (`Primary Blue/06`, `WB Brand/White`, `Drop Shadow/02-Cards`, `Label/L`, `Text/Link`, `Generic/White`) | Single source of truth / no foreign tokens in brand library | Remove/relocate non-`lift` systems out of the WBG library, or namespace them as explicitly out-of-scope. NOT imported into theme.css. **RESOLVED — brand owner chose to remove from library entirely (2026-06-24); Figma library-hygiene only, no code impact.** | resolved | [#12](https://github.com/kharloridado/wbg/issues/12) |
| FND-002 | design-token | high | Type page (node:10995-7259) + colors (node:11122-2143) | Ambiguous brand font: library exposes Inter, Open Sans, Poppins, Manrope, Andes, IBM Plex Sans. Even within `lift`: `lift/global/font family`=Open Sans vs `lift/global/font-family/body`=Inter | One canonical body + heading font per brand | **RESOLVED — brand owner chose Open Sans (2026-06-17).** typography.css set to Open Sans (body/heading/label); matches the primitive Open Sans type styles. | resolved | n/a (decided pre-filing) |
| FND-003 | design-token | medium | Effects (node:19737-9489) vs colors (node:11122-2143) | `lift/effects/shadow/default` = `#21262d29` on effects page but `#000d1a29` on colors page | Token must resolve to one value (per mode) | Reconcile the shadow color to a single value. Built with effects-page `#21262d29`. **RESOLVED — brand owner confirmed `#21262d29` (effects page) canonical (2026-06-24); reconcile the colors-page `#000d1a29` to match. No code change.** | resolved | [#13](https://github.com/kharloridado/wbg/issues/13) |
| FND-004 | design-token | medium | Effects (node:19737-9489) + type (node:10995-7259) | Radius naming/value mismatch: `lift/border radius/radius-4`=16px (name implies 4), `lift/border radius/base`=4px, `Border Radius/Medium`=8, `Border Radius/Large`=16 | Consistent, non-misleading token names | Rename `radius-4`→`radius-large`/`-16`; align the `lift` and primitive radius names. Built as base 4 / medium 8 / large 16. **RESOLVED — brand owner accepted Figma names as-is; mapping documented (base 4 / medium 8 / large 16). Won't-fix, no code change (2026-06-24).** | resolved | [#14](https://github.com/kharloridado/wbg/issues/14) |
| FND-005 | design-token | low | Spacing System (node:10994-5086) | `Spacing/xtiny` = 2px breaks the 4pt base grid | Project spacing base = 4pt | ~~Withdrawn — 4pt grid was an unverified config assumption, not a confirmed The Loop rule. `xtiny=2px` built faithfully; issue #15 closed as not-planned (2026-06-17).~~ | withdrawn | [#15](https://github.com/kharloridado/wbg/issues/15) (closed) |
| FND-006 | design-token | low | Semantic Colors (node:11122-2879) | Orphan semantic tokens with no primitive: `Text/On Light/Subdued` `#000d1a91`, `Text/On Blue` `#004099`, `Text/On Light/Link/Hover` `#066db1` (light); `Divider/On Dark/Default` `#00538a99` (dark, deferred) | Semantic tokens should reference a primitive | Add matching primitives or retarget these roles. Built faithfully as literals; the 3 light orphans are in semantic-colors.css, the dark one in deferred semantic-colors-dark.css. **RESOLVED — brand owner accepted the orphans as literals (2026-06-24); no primitives added, no code change.** | resolved | [#16](https://github.com/kharloridado/wbg/issues/16) |
| FND-007 | design-token | low | Semantic Colors (node:11122-2879) | `Domain/States/informational/*` is lowercase while every sibling state group (`Positive`/`Negative`/`Warning`/`Suggestion`/`Disable`) is title-case | Consistent token casing | Rename to `Informational`. Built as `--color-domain-state-info-*`. **RESOLVED — brand owner accepted the lowercase name as-is (2026-06-24); no code impact (already normalized).** | resolved | [#17](https://github.com/kharloridado/wbg/issues/17) |
| FND-008 | consistency | low | Semantic Colors (node:11122-2879) | One role spelled many ways across groups: "info" = `informational`(Domain)/`Information`(Background)/`Informational`(Outline,Icon)/`Info`(Text); "suggestion" = `Suggestion`(Domain,Background)/`Suggestional`(Outline,Icon)/`Suggest`(Text) | One canonical name per role across all groups | Standardise on `informational` and `suggestion` everywhere. Built with normalised `-info-`/`-suggest(ion)-` token names. **RESOLVED — brand owner accepted the mixed spellings as-is (2026-06-24); no code impact (already normalized).** | resolved | [#18](https://github.com/kharloridado/wbg/issues/18) |
| FND-009 | brand/token | medium | Palette / Accent·Indigo (node:11122-2143) | Ramp inversion: `Indigo/40` `#9fa9f7` is darker than `Indigo/50` `#a8b2ff` (step 50 should be ≥ step 40 in darkness) | Ramps must be monotonic light→dark | Swap the 40/50 values or re-step the Indigo ramp. Built faithfully as published. **RESOLVED — brand owner accepted the published values as-is (2026-06-24); no swap/re-step, no code change.** | resolved | [#19](https://github.com/kharloridado/wbg/issues/19) |
| FND-010 | design-token | low | Palette / Green + Yellow (node:11122-2143) | Step naming mixes numeric steps with `$lift-core … base`/`on-dark` raw names (`green-on-dark-07`, `green-base`, `yellow-base`, `yellow-on-dark-base`) — non-uniform vs Blue/Red/Purple's 10–90 scale | Uniform step naming across ramps | Renumber Green/Yellow to a 10–90 scale. Built faithfully with source names. **RESOLVED — brand owner accepted source names as-is (2026-06-24); no renumber, no code change.** | resolved | [#20](https://github.com/kharloridado/wbg/issues/20) |
| FND-011 | a11y/contrast | medium | Semantic Colors / `Domain·Interactive·Hover` + `Domain·Secondary` = `Blue/40` `#169af3` (node:11122-2879) | `#169af3` on white = **3.02:1** — below the 4.5:1 normal-text minimum (meets 3:1 for large text / UI components) | WCAG 2.2 SC 1.4.3 | Where used as interactive/link *text*, darken to `Blue/50 #0071bc` (5.14:1). Built faithfully; flag for sign-off. **CLOSED — not reproduced (2026-06-24).** Build audit shows Blue/40 is never applied as interactive/link *text*: real uses are button/switch **background** fills (dark labels on top, cf. FND-014), the text-button hover **underline decoration** (the label itself stays Blue/70, 10.31:1), and UI **borders/icons** (≥3:1). The only `color: var(--color-blue-40)` is the generated `.text-blue-40` primitive utility (`tokens/color-utilities-primitives.css`), unused on text in the Style Guide. The 4.5:1 normal-text concern doesn't materialize; issue #21 closed. | closed (not reproduced) | [#21](https://github.com/kharloridado/wbg/issues/21) (closed) |
| FND-012 | a11y/brand | medium | tokens/outsystems-ui-overrides.css · `--color-focus-outer` (OutSystems UI v2.28.1 focus 'outer') | Brand-inheritance retints OutSystems' high-contrast yellow focus ring `#ffd337` to The Loop's focused role `Blue/50 #0071bc`; a blue ring on a blue surface may fail to be perceivable. **Now concrete (2026-06-21):** Layout Top `--header-color` set to `var(--color-primary)`, so the menu-link focus rings (Blue/50) now sit directly on a blue header — see tokens/outsystems-ui-header.css. | WCAG 2.2 SC 2.4.11 / 2.4.7 | Keep Blue/50 on light surfaces but retain a high-contrast ring on brand/dark surfaces (e.g. a white ring on the Layout Top header), or add a dedicated brand focus-ring token, or sign off the trade-off. Built faithfully (Blue/50). **RESOLVED — brand owner signed off the trade-off (2026-06-24); planned follow-up: Layout Top header → white later, which puts Blue/50 on a light surface and resolves the concern naturally. No code change. Update 2026-07-04: the Layout Top header branding itself was removed by user ruling (default OutSystems header) — the dark-header concrete case no longer exists.** | resolved | [#23](https://github.com/kharloridado/wbg/issues/23) |
| FND-013 | design-token | low | Button (node:15597-766) — `loop/button/gap`=6, `loop/button/label/letter spacing`=-0.5 | Two button values fall off the documented foundation scales: gap `6px` is off the 4pt spacing grid; label tracking `-0.5px` is not on the `-0.35`/`-1.5` letter-spacing scale | Component values should resolve to documented scale tokens | Add to scale (e.g. round gap to 8/4) or confirm the off-scale values are intentional. Built faithfully as `--space-button-gap: 6px` / `--letter-spacing-button: -0.5px`. | open (register-only, low) | n/a |
| FND-014 | a11y/contrast | medium | Button (node:15597-766) — secondary/ghost HOVER fill | On `:hover`, secondary fills `Blue/40 #169af3` and ghost fills `Blue/20 #cde7f9` while the label stays `Blue/70 #004370`. Blue/70 on Blue/40 ≈ 2.9:1 — below the 4.5:1 normal-text minimum (related to FND-011) | WCAG 2.2 SC 1.4.3 | Confirm the intended hover label color (e.g. switch label to white on the filled hover state) or darken the fill. Built faithfully (label stays Blue/70). **RESOLVED & IMPLEMENTED — stakeholder spec (2026-06-24): secondary HOVER = bg blue-40 / text blue-90 / border blue-40 (≈5.1:1); PRESSED = bg blue-30 / text blue-70 / border blue-70 (≈6.3:1) — both pass AA. Applied in `src/blocks/loop-button.css`; handover re-embedded. Ghost hover already passed, unchanged.** | resolved (code changed) | n/a |
| FND-015 | design-token | low | Button Text (node:15597-4652) — `Keyboard Focus/On Light`=`#b6490c`, `Keyboard Focus/On Dark`=`#f5926c` | The keyboard-focus ring colors have no primitive in the orange ramp (nearest `accent-orange-70 #b12911` ≠ `#b6490c`) | Semantic tokens should reference a primitive | Add matching orange primitives or retarget the focus roles. Built faithfully as literal `--color-focus-keyboard-on-light: #b6490c` (on-dark deferred to dark phase). | open (register-only, low) | n/a |
| FND-016 | a11y/contrast | medium | Checkbox (node:19336-17679) — unchecked box border `Outline/On Light/Default` `#00396b3d` | The unchecked checkbox boundary `#00396b3d` over white ≈ **1.45:1**, below the 3:1 minimum for the boundary of an unselected component | WCAG 2.2 SC 1.4.11 (non-text contrast) | Darken the unchecked border for the resting state. **Correction (2026-06-24): the earlier-suggested `Outline/On Light/Emphasis` (`neutral-alpha-42`) only reaches 2.53:1 and still FAILS the 3:1 floor.** Genuinely passing targets: `blue-70 @70% #004370b3` (4.55:1), `neutral-50 #586e84` (5.28:1), or `blue-70 @55% #0043708c` (3.07:1, borderline). Built faithfully as `--color-outline-on-light-default` (= `neutral-alpha-24 #00396b3d`, 1.57:1). **2026-06-24: stakeholder elected NOT to change code; logged for designer decision with the passing options presented — A `neutral-50 #586e84` (5.28:1), B `blue-70 @70% #004370b3` (4.55:1), C `blue-70 @55% #0043708c` (3.07:1, borderline), D keep `#00396b3d` as-is (won't-fix). One ruling resolves both (checkbox + text field, FND-019).** | open (awaiting designer) | n/a |
| FND-017 | consistency/a11y | low | Checkbox (node:19336-17679) — "Required" state | The "Required" state renders in error-red (`#9d161d`), identical to the Error type, before any user interaction — a resting required field reads as a validation error | A resting/required control should be visually distinct from an error/invalid control | Give Required its own resting treatment (e.g. a required marker on the label) distinct from the error-red box. Built faithfully (Required == Error red). | open (register-only, low) | n/a |
| FND-018 | design-token | low | Text Field (node:19336-9606) — `loop/field/vpadding`=18, `loop/label/gap`=6, disabled border | Field metrics fall off the foundation scales: `vpadding 18px` and label `gap 6px` are off the 4pt grid (cf. FND-013); the disabled border references `Outline/On Dark/State/Disable` `#ffffff7a`, an on-dark token with no light-mode primitive | Component values should resolve to documented scale tokens / a light primitive | Add to scale or confirm off-grid; add a light disabled-border primitive (or render borderless). Built faithfully as `--loop-field-padding-block: 18px` / `--loop-field-label-gap: 6px`; disabled rendered borderless via the disable fill. | open (register-only, low) | n/a |
| FND-019 | a11y/contrast | medium | Text Field (node:19336-9606) — resting input border `Outline/On Light/Default` `#00396b3d` | The resting field boundary `#00396b3d` over white ≈ **1.45:1**, below the 3:1 minimum for the boundary of an unselected component (same token/issue as the checkbox, FND-016) | WCAG 2.2 SC 1.4.11 (non-text contrast) | Darken the resting input border. **Correction (2026-06-24): the earlier-suggested `Outline/On Light/Emphasis` (`neutral-alpha-42`) only reaches 2.53:1 and still FAILS the 3:1 floor.** Genuinely passing targets: `blue-70 @70% #004370b3` (4.55:1), `neutral-50 #586e84` (5.28:1), or `blue-70 @55% #0043708c` (3.07:1, borderline). Built faithfully as `--color-outline-on-light-default` (= `neutral-alpha-24 #00396b3d`, 1.57:1). **2026-06-24: stakeholder elected NOT to change code; logged for designer decision — same disposition as FND-016 (passing options A `neutral-50` 5.28:1 / B `blue-70 @70%` 4.55:1 / C `blue-70 @55%` 3.07:1 / D keep as-is); one ruling on `--color-outline-on-light-default` resolves both (checkbox + text field). Remains open.** | open (awaiting designer) | n/a |
| FND-020 | consistency | low | Text Field (node:19336-9606) — placeholder vs semantic "subdued" | The field placeholder resolves `Text/On Light/Subdued` = `#00294d91` (neutral-alpha-57), but the registered semantic `--color-text-on-light-subdued` = `#000d1a91` (FND-006). "Subdued" carries two different values across the library | One canonical value per semantic role | Reconcile the two "subdued" values to one. Built the placeholder faithfully on the Figma value (`--color-neutral-alpha-57`). | open (register-only, low) | n/a |
| FND-021 | consistency | low | Text Field (node:19336-9606) — size system | The component documents sizes two ways: the "Sizes" DS note says **3** (Large [default] / Normal / Small) while the size showcase shows **4** (xLarge / Large / Regular / Small), and the assembled default component is xLarge — so the default size is ambiguous | One documented size set with an unambiguous default | Reconcile to one size set and name the default. Built all 4 with **Regular as the default = base** (per stakeholder direction, aligning the whole form-control family — cf. Checkbox FND-038): xLarge = `.input-xlarge`, Large = `.input-large`, Regular = `.input-regular` (explicit alias of the base), Small = `.input-small`. **2026-07-02: the bare `.input-*` classes now also step the input/placeholder text 16/14/13/12 (Figma 19336-9729) via `--loop-field-text-size`, matching the `.loop-field--*` wrapper modifiers — one size system for bare and wrapped fields. The `--loop-field-text-size` token default is now 13px (was 16px), so an unsized input renders the full Regular spec per the Regular-as-default direction.** | open (register-only, low) | n/a |
| FND-022 | design-token | low | Radio Button (node:19336-18637) — size metrics | Component values fall off the foundation scales: group `vpadding 14px` is off the 4pt grid (cf. FND-013); the Regular label `13px`/`15px` line-height is not on the documented `12/14/16/18` type scale; the Small label `letter-spacing 0.25px` is off the documented letter-spacing scale | Component values should resolve to documented scale tokens | Add to scale or confirm the off-scale values are intentional. Built faithfully (`--space-checkbox-group-row: 14px`; literal `13px/15px`; literal `0.25px`). | open (register-only, low) | n/a |
| FND-023 | consistency/a11y | low | Radio Button (node:19336-18637) — "Required" state | The "Required" state renders in error-red (`#9d161d`), identical to the Error/invalid state, before any user interaction — so a resting required group reads as a validation error, and "required" is conveyed by color alone on the control (asterisk/helper live on the Form field, not the radio). Same pattern as checkbox FND-017 | A resting/required control should be visually distinct from error/invalid (WCAG 2.2 SC 1.4.1) | Give Required its own resting treatment (required marker on the label) distinct from the error-red ring. Built faithfully (Required == Error red via `.not-valid`). | open (register-only, low) | n/a |
| FND-024 | design-token | low | Toggle (node:25344-27822) — label line-height | The toggle label line-height is published twice with different values: `loop/Toggle/Label/Line Height`=16 vs `loop/toggle/label/line height`=18 (same casing-divergence pattern as FND-008) | One canonical value per token | Reconcile to a single line-height. Built on `18` (the value the assembled component instance uses) as `--loop-toggle-label-leading: 18px`. | open (register-only, low) | n/a |
| FND-025 | design-token | low | Toggle (node:25344-27822) — switch size metrics | Only the xLarge switch exposes `loop/toggle/switch/*` tokens (width 56 / height 32 / circle 24 / padding 4). Large/Regular/Small show track dimensions (48×26 / 40×20 / 32×16) but no per-size circle/padding tokens | Every documented size should resolve to published metric tokens | Publish circle/padding tokens per size, or confirm padding is a constant 4px. Built xLarge from tokens; smaller sizes from the observed track dims with padding held at 4px (circle = height − 2·padding). | open (register-only, low) | n/a |
| FND-026 | consistency | low | Toggle (node:25344-27822) — ON thumb check glyph | The checked+enabled thumb carries a 16px check glyph [node:25845-30789] whose fill colour isn't published as a variable/token (the other states have no glyph) | Component visuals should resolve to tokens/assets | Publish the glyph colour token (or export the asset). **Update 2026-07-05:** glyph now built (Sizes ref 25862-14729 shows it at every size) as FA 6 Pro solid check `\f00c`, box = thumb − 2×padding (16/14/12/8), colour **inferred** as `Domain/Interactive/Enabled Primary` #004370 (visual match to the ref render; muted to `interactive-disable` when disabled) — `loop-switch.css` + `component-toggle.css`. Keep open until the colour token is published/confirmed. | open (register-only, low) | n/a |
| FND-027 | design-token | low | Tooltip (node:17874-7113) — light-mode drop-shadow | Light variant `drop-shadow(0px 2px 2px rgba(0,0,0,0.1))` has no matching scale token: `--shadow-low` = `0 2px 8px` (blur differs, color differs); `--shadow-lowest` = `0 0 8px` (no y-offset) | Component shadow values should resolve to a published shadow scale token | Add a shallow 2px-blur shadow entry to the shadow scale (or confirm the exact value as a Tooltip-specific token). Built faithfully as `--loop-tooltip-light-shadow: 0px 2px 2px rgba(0,0,0,0.1)`. | open (register-only, low) | n/a |
| FND-028 | consistency/design-token | low | Tag (node:17313-5502) — `loop/tag/border radius`=48 | The Tag pill radius is `48px`, but the foundation pill token `--radius-pill` = `32px`. Both fully round the tag heights (24–48px), so they're visually identical here, but the value drifts from the documented scale (same family as FND-004) | Component values should resolve to a documented radius token | Reconcile to `--radius-pill` (32) or publish a `--radius-pill-48`/document 48 as intentional. Built faithfully as `--loop-tag-radius: 48px`. | open (register-only, low) | n/a |
| FND-029 | design-token | low | Tag (node:17313-5502) — size metrics | Tag size system isn't fully tokenised: a single label size (`16`) + hpadding (`12`) are published, plus two vertical paddings (`loop/Tag/vPadding`=6.5 — off the 4pt grid, cf. FND-018 — and `loop/tag/vpadding`=8); the four heights (24/32/40/48) map onto the spacing scale but have no per-size vpadding tokens (Large/xLarge unspecified) | Every documented size should resolve to published metric tokens | Publish per-size vertical-padding tokens (and confirm/round the 6.5px value). Built faithfully: heights from `--space-regular/medium/large/xlarge`, small vpadding as literal `6.5px`. **RESOLVED (2026-06-25): Tag heights are now pinned as a fixed `height` per size with the label flex-centred (matching the Badge-Status FND-037 pattern + the border-no-height-shift rule), so NO vertical padding is applied at all — the off-grid 6.5px vpadding is retired and the per-size vpadding-token gap is moot.** | resolved | n/a (register-only) |
| FND-031 | consistency/design-token | low | Dropdown / Select (node:18787-4817) — field metrics vs Text Field & scale | The Select field diverges from its sibling Text Field and the documented scales: ~~corner radius = `--radius-pill` (32px) vs the Text Field's `--radius-medium` (8px)~~ **RADIUS RESOLVED** (see note); `vpadding` 11px is off the 4pt grid (cf. FND-013/FND-018); label & value text is 13px while the Text Field and the Multi-select both use 16px (`loop/label/font size`) | Sibling form controls should share corner-radius + type metrics; component values should resolve to documented scale tokens | **2026-06-22 — radius reconciled per stakeholder direction:** both the Text Field/textarea (`--loop-field-radius`) and the Select (`--loop-select-radius`) now default to `--radius-pill` (32px, "Modern" mode), so sibling form controls share one corner radius. Remaining open: confirm the pill + 13px text are an intentional Select treatment; put 11px on the spacing scale. Built faithfully: `--loop-select-radius: var(--radius-pill)`, `--loop-select-padding-block: 11px`, `--loop-select-label-size/text-size: 13px`. **2026-07-04 note: the 2026-06-22 pill default was itself superseded by issue #130 (commit 443a494) — the field family default is now `--radius-medium` (8px) with `.loop-field--rounded` as the pill opt-in, so the shipped default renders 8px while the Figma ref still shows 32px; that delta is covered by the #130 stakeholder decision, not a fidelity failure (spotted by the 2026-07-04 frozen-ref re-audit).** | open (radius resolved; type/padding register-only, low) | n/a |
| FND-032 | design-token | low | Dropdown / Select (node:18787-4817) — popup-list border colour | The open list border in Figma is `#dae1e8` (a `lift/outline/on light/low` token); WBG has no matching semantic primitive (nearest registered roles are `--color-outline-on-light-subdued` neutral-alpha-16 and `--color-outline-on-light-dimmed` neutral-alpha-08) | Semantic values should resolve to a published WBG primitive, not a foreign `lift` token (hard rule #3) | Add a light "outline-low" primitive matching `#dae1e8`, or confirm the substitution. Built faithfully via the nearest token: list border = `--color-outline-on-light-subdued`. | open (register-only, low) | n/a |
| FND-033 | design-token | low | Button Text (node:15597-4652) — at-rest underline decoration | The link/text-button underline at rest is a raw `rgba(0,67,112,0.55)` (blue-70 @ 55%) set directly on the text layer — no Figma variable / no WBG primitive (hover `#169af3`=Blue/40 and disabled `#dae3eb`=Neutral/15 do resolve to primitives) | Decoration colours should resolve to a published primitive/semantic token (hard rule #3) | Add a blue-70-alpha primitive (or a named link-underline token) and retarget. Built faithfully as the literal semantic `--color-text-on-light-link-underline-enabled: #0043708c`. | open (register-only, low) | n/a |
| FND-030 | design-token | low | Tag (node:17313-5502) — selected state | The selected (filled) state colours aren't cleanly published per colour: text resolves to an on-dark token (`semantic-colors-dark.css`, deferred/not imported), and per-colour selected backgrounds are inferred from the `Domain/State/*/High` roles rather than a tag-specific `selected` token | Selected-state visuals should resolve to published tokens for the light theme | Publish tag `selected` bg/text tokens (and a light-theme on-dark text primitive). Built faithfully: selected bg = `--color-domain-state-*-high`, text = `--color-white` pending confirmation. | open (register-only, low) | n/a |
| FND-034 | design-token/consistency | low | Card (node:20315-6122) — card drop-shadow | The Loop card effect `-loop shadows/cards` is published with `x/y/blur/spread` all = `0` (color `effects/shadow/default` `#00396b29`), so the card has **no visible elevation** — yet OutSystems UI's default Card ships a real shadow. The "Classic (Dashboard) vs Modern (External Web — no shadow)" labels suggest the flat look is deliberate for external web | A card shadow token should resolve to a usable elevation, or the flat intent should be documented | Confirm the zeroed shadow is intentional (and document a "classic/dashboard" elevation separately) or publish real offset/blur. Built faithfully **flat** (`--loop-card-shadow: none`); optional `--loop-card-shadow-elevated: var(--shadow-low)` for dashboard contexts. **RESOLVED 2026-07-04 — the new dev-specs node 20315-6129 publishes `card/shadow` as a styles-collection switch: classic/mixed = `0 8 20 0` `effects/shadow/default`, modern (external web) = none. Card rebuilt as the simple `.loop-card` with the classic shadow by default + `loop-card--no-shadow` for modern.** | resolved (design clarified) | n/a |
| FND-036 | brand/a11y | medium | Badge Status (node:18153-7159) — label font-weight | The label style `UI Component/.Badge/Status/Label` publishes `font-weight: 500` (token `loop/badge-status/label/font-weight`) but is named style "SemiBold". The theme self-hosts Open Sans `400/600/700` only (`typography.css`), so a `500` request resolves to the `400` face — the label renders **Regular**, not the intended semibold, weakening status legibility | Type weights should resolve to a shipped font face; the rendered weight should match the design intent (hard rule #4 — no silent substitution) | Confirm intent: either change the token to `600` (SemiBold, already shipped) or self-host an Open Sans `500` face. Built faithfully: `--loop-badge-status-label-weight: 500` (verbatim). **RESOLVED & IMPLEMENTED — stakeholder chose to self-host the Open Sans 500 (Medium) face (2026-06-24); `typography.css` now ships the 500 `@font-face` + `--font-weight-medium: 500`, and `--loop-badge-status-label-weight` resolves to it, so the label renders real Medium (the token value 500 is honoured). The Figma style-name mismatch ("SemiBold" name on a 500 value) is still raised to the designer to rename → Medium. SETUP: upload `open-sans-500.woff2` to ODC Resources (module TheLoopTheme).** | resolved (code changed) | n/a |
| FND-037 | design-token | low | Badge Status (node:18153-7159) — headline pill height | Figma publishes only the xlarge vertical padding (`loop/badge-status/v-padding` = 10); no per-size headline height token exists. To keep the pill height deterministic in any host (ODC inflates an inherited line-height, so a padding-driven height drifts +2px vs the preview), the headline height is **pinned per size to the measured Figma pill heights** (xlarge 40 / large 32 / regular 24 / small 22) | Per-size component metrics should resolve to a published token, not be measured off frames | Publish per-size headline height (or v-padding) tokens. Built faithfully via `--loop-badge-status-*-headline-h` in `component-badge-status.css`. | open (register-only, low) | n/a |
| FND-035 | consistency | low | Card multimedia (node:20315-7404) — on-dark roles | The multimedia/hero card uses **On-Dark** text + a dark scrim over the image (`Text/On Dark/Default` `#ffffffbf`, `Text/On Dark/Emphasis` `#ffffffe5`, scrim → `#012740`). The semantic On-Dark role layer is parked in `semantic-colors-dark.css` (not imported into the light build), so the restyle references the mode-agnostic `--color-gray-alpha-white-75/-90` and `--color-blue-90` **primitives** directly | On-dark component colours should resolve to semantic On-Dark roles, not raw primitives | Re-point the multimedia tokens at semantic On-Dark roles when the dark-mode phase imports them. Built faithfully on the primitives (`--loop-card-multimedia-eyebrow/-title/-scrim`). **SUPERSEDED 2026-07-04 — the bare-native Card family restyle (incl. the multimedia hero) was reverted; Card scope is now the simple `.loop-card` shell only (nodes 20315-6129 / 20315-6189 / 20376-15012), and the multimedia tokens left the codebase with it.** | superseded (scope removed) | n/a |
| FND-038 | consistency | low | Checkbox (node:19336-17679) — size system & default | The "How To Use" note documents **3** sizes (SMALL/LARGE/REGULAR) while the "Variants"/"Sizes" frames show **4** (XLarge/Large/Regular/Small), and the Figma-assembled default instance ships as **xLarge** — so the documented size set and the default size are ambiguous (same pattern as Text Field FND-021). Separately, sub-24px Regular (20) / Small (16) controls fall under the WCAG 2.2 SC 2.5.8 (AA) 24px target minimum | One documented size set with an unambiguous default; interactive targets ≥ 24px (or qualify for an exception) | Reconcile to one size set and name the default. Built all four with **Regular as the default** per stakeholder direction; sub-24px controls rely on the SC 2.5.8 spacing exception (group row gap ≥ 8px) + the associated label hit-area. Off-scale size metrics tracked under FND-013/FND-022. | open (register-only, low) | n/a |
| FND-039 | design-token | low | Badge / Label (node:22966-4806) — colour palette | Five of the six badge colour families — `indigo` (#d8d9fc/#3c40f0), `teal` (#cfe9ef/#026e89), `purple` (#e4dafb/#6427ef), `red` (#f4dee4/#b4234d), `magenta` (#f2d6f6/#a028ad) — have **no primitive in the foundation ramp** (colors.css, which only carries blue/green/yellow/purple/red/neutral/accent-* ramps). The badge `purple`/`red` also differ from the existing Tag/State `purple` (#763ba9/#f1e1ff) and `red` roles. Separately, the native OutSystems UI Tag colour enum doesn't map 1:1 to this palette, so the `.tag` restyle approximates `Blue→indigo`, `Green→teal` | Component colours should resolve to a published foundation primitive (hard rule #3); one hue should have one value across the library | Add the badge hues (indigo/teal/magenta + the badge purple/red) to the foundation ramp, or confirm them as Badge-only literals; reconcile the two purples/reds. Built faithfully as component literals in `component-badge-label.css`; native Tag colour classes mapped to the nearest hue. | open (register-only, low) | n/a |
| FND-040 | brand/a11y | medium | Badge / Label (node:22966-4806) — label font-weight | The label style `UI Component/.Badge/Label/Label` publishes `font-weight: 500` (token `loop/badge-label/label/font-weight`) but is named style "SemiBold". The theme self-hosts Open Sans `400/600/700` only (`typography.css`), so `500` resolves to the `400` face — the badge label renders **Regular**, not SemiBold (identical root cause to Badge Status FND-036) | Type weights should resolve to a shipped font face; the rendered weight should match design intent (hard rule #4 — no silent substitution) | Confirm intent: change the token to `600` (SemiBold, already shipped) or self-host an Open Sans `500` face. Built faithfully: `--loop-badge-label-weight: 500` (verbatim). **RESOLVED & IMPLEMENTED — same ruling as FND-036 (2026-06-24): the Open Sans 500 (Medium) face is now self-hosted (`typography.css` + `--font-weight-medium: 500`), and `--loop-badge-label-weight` resolves to it, so the badge label renders real Medium. The "SemiBold" style-name mismatch is still raised to the designer. SETUP: upload `open-sans-500.woff2` to ODC Resources (TheLoopTheme).** | resolved (code changed) | n/a |
| FND-041 | a11y/contrast | low | Badge / Status (node:18153-7159) — headline pill border, not-started `#bdccdb` (neutral-30) & in-progress `#d4dee8` (neutral-20) | The headline pill boundary `#bdccdb` (≈ **1.5:1**) and `#d4dee8` (≈ **1.2:1**) over white fall below the 3:1 minimum for the boundary of a component (same family as checkbox/field FND-016/019) | WCAG 2.2 SC 1.4.11 (non-text contrast) | No change to source — state meaning is carried by the visible label + indicator dot, not the pill border alone, so the design is conformant in practice. Logged for register completeness. Built faithfully via inset box-shadow (zero layout). | open (register-only, low) | n/a |
| FND-042 | a11y/contrast | high | System Alert — `offline` type, white text on `--loop-sysalert-offline-bg` `#8a9db1` (neutral-40): title/icon/dismiss = white-75 `#ffffffbf`, message/action = white-90 `#ffffffe5` | White-75 over `#8a9db1` ≈ **2.23:1** and white-90 ≈ **2.6:1**, both failing WCAG 2.2 SC 1.4.3 (4.5:1 for normal text) and the 3:1 large-text/non-text floor. Affects title, message, action, icon and dismiss glyphs on the offline alert | WCAG 2.2 SC 1.4.3 (text contrast) | Darken the offline background (neutral-60 `#4b5e71` → white-75 4.62 / white-90 5.80, or neutral-70 `#3d4c5c` → 5.79 / 7.51) or use a darker text role (blue-90 `#012740` → 5.52). Built faithfully — `#8a9db1`, white-75 and white-90 left unaltered; disclosed only (hard rule #4). **2026-06-24: stakeholder elected NOT to change code; raised to designer and filed as GitHub Bug per the high-sev gate. Build stays faithful (`#8a9db1` + white-75/90).** 2026-07-04: per-layout rebuild (FND-048) moves the **multi-line** offline message to white-75 ≈ 2.22:1 (was white-90 ≈ 2.55:1) — still within this finding's scope. | open (awaiting designer) | [#74](https://github.com/kharloridado/wbg/issues/74) |
| FND-043 | consistency/fidelity | medium | Button & Button Group — default `min-height` (Figma default instance = xLarge 56px) | The Figma default Button/Button-Group instance is **xLarge (56px)**, but the implementation ships **Regular (40px)** as the default to align the button family with the form-control default direction (parallel to Text Field FND-021 / Checkbox FND-038). The Button-Dropdown trigger still encodes 56px | Default size should trace to the design's default instance, or the reconciliation must be disclosed (no silent default change) | Confirm Regular/40 as the intended family default (then this resolves), or restore xLarge/56. Built faithfully as Regular/40 per stakeholder direction; reconciliation now disclosed in both handovers. | open (register-only, medium) | n/a |
| FND-044 | design-token | low | DatePicker (Flatpickr balloon) — day cell marker radius | No picker-specific radius token is published, so the day marker uses `--radius-pill` (32px → circle/stadium), inheriting the Select pill family (cf. FND-031). The Select pop-up *list* it otherwise mirrors uses `--radius-medium` (8px) corners, so the picker's round day markers are a deliberate intra-component choice rather than a documented token | Component values should resolve to a documented, component-appropriate radius token | Publish a DatePicker day-marker radius token (confirm the round marker) or reconcile to a documented value. Built faithfully as `--loop-datepicker-day-radius: var(--radius-pill)`. **2026-06-26: RESOLVED — the published Figma DatePicker node (17897:9088, day variants 17897:7144) draws the day marker as a circle, confirming `--radius-pill`. No longer a divergence.** | resolved (design-confirmed) | n/a |
| FND-045 | a11y/design-token | low | DatePicker (Flatpickr balloon) — "today" resting marker | OutSystems' own Flatpickr theme marks "today" with a neutral outline (`neutral-5/-6` family); on white that boundary sits below the 3:1 non-text minimum (same family as FND-016/019). The Loop instead rings "today" with `--color-outline-on-light-link-enabled` (Blue/70) — the only resting outline token that clears 3:1 — which leans on the brand colour to carry the state | A resting state marker should be both ≥3:1 and resolve to a documented token; today should be distinguishable from the selected (Blue/70 fill) state | Publish a dedicated "today" marker token that clears 3:1, or confirm the Blue/70 ring. Built faithfully as `--loop-datepicker-today-border: var(--color-outline-on-light-link-enabled)`. **2026-06-26: RESOLVED — the published Figma DatePicker node (17897:9088, "Today=Yes" day variants) marks today with the Blue/70 (`#004370`) ring, confirming the brand-ring choice. No longer a divergence.** | resolved (design-confirmed) | n/a |
| FND-046 | design-token/consistency | low | Modal (node:22049-35100) — dialog drop-shadow | The Figma "-loop modal" sticker carries **no drop-shadow effect**, so the dialog reads **flat** over the dark overlay — yet OutSystems UI's default Popup widget ships `--shadow-xl`. Identical in kind to the Card flat-shadow departure (FND-034); the modal sits on a `#012740e5` (blue-90 @ 90%) overlay where elevation is largely moot | A dialog shadow token should resolve to a usable elevation, or the flat intent should be documented | Confirm the no-shadow modal is intentional (consistent with the Card's "Modern / External Web — no shadow" treatment) or publish real offset/blur. Built faithfully **flat** (`--loop-modal-shadow: none`). | open (register-only, low) | n/a |
| FND-047 | design-token | low | Modal (node:22049-35100) — dialog & header dimensions | Per-component modal metrics have no foundation token: dialog `width` Large `960` / Small `456`, `icon-size` `24`, `close-size` `40`, `close-glyph` `16`. They live as literals inside the `--loop-modal-*` declarations and are consumed via `var()` (same pattern/disposition as Badge-Status FND-037) | Per-size/per-component metrics should resolve to a published token, not a literal | Publish modal width/size tokens (or confirm them as Modal-only literals). Built faithfully via `--loop-modal-width`/`-width-sm`/`-icon-size`/`-close-size`/`-close-glyph` in `component-modal.css`. | open (register-only, low) | n/a |
| FND-048 | consistency | low | System Alert (node:17873-7603) — per-element text colours differ between single- and multi-line | For the same type, Figma assigns different message/action shades by layout: error message = white-75 (single) / white-90 (multi); error action = `#f5f7f9` (single) / white-90 (multi); informative action = white-90 (single) / white `#ffffff` (multi); offline message = white-90 (single) / white-75 (multi). The warning title is also published as white-75 in dev-mode but renders dark `#473201`. So a single semantic element (e.g. "message") carries two values across layouts | One canonical value per semantic element/role (cf. FND-008/FND-020 colour-role consistency) | Reconcile each element to one colour across layouts. ~~Built normalized to the single-line values; warning title built dark `#473201`.~~ **2026-07-04: rebuilt faithfully per-layout** per stakeholder direction (FND-062 Alert precedent) — multi-line deltas carried by `--loop-sysalert-{error-message,error-action,informative-action,offline-message}-multi`; icons/dismiss to pure white (`Icon/On Dark/Emphasis`); warning title now white-75 per the current sticker render (contrast raised as FND-067). Layout variance itself still awaiting designer reconciliation. | open (register-only, low) | n/a |
| FND-049 | design-token | low | Search (node:17191-8728) — leading glass icon fill | The magnifying-glass fill is the Figma "Icon/Default" `#000000`, which has no neutral primitive in the foundation ramp; the mock renders it muted (grey), not pure black | Component icon colours should resolve to a published primitive/semantic token (hard rule #3) | Publish a search-glass icon token or confirm the muted rendering. Built faithfully on the nearest registered role `--color-icon-on-light-default` (neutral-60) as `--loop-search-icon-color`. | open (register-only, low) | n/a |
| FND-050 | consistency/a11y | low | Search (node:17191-8728) — Filled-state clear (×) | The Figma "Filled" state shows a clear (×) button, but the native OutSystems Search widget ships no clear affordance. Delivered via the inner input's `type=search` cancel button, which is rendered by **WebKit/Blink only** — Firefox shows no clear button, so the affordance is absent there | A documented control affordance should be present across supported browsers | Confirm WebKit/Blink-only is acceptable, or add an explicit clear element/JS. Built faithfully via `::-webkit-search-cancel-button` styled to the Loop × in `loop-search.css`. | open (register-only, low) | n/a |
| FND-051 | consistency | low | Search (node:17191-8728) — size system | The native OutSystems Search widget exposes no size variants, yet Figma documents 4 (xLarge/Large/Regular/Small). The sizes are delivered by reusing the Text Field's `.input-*` classes on the inner `.form-control[data-input]` (heights 56/48/40/32 match the Text Field exactly); default = Regular (40px), inheriting the family default + ambiguity of FND-021 | Sibling form controls should share one size system + an unambiguous default | Confirm Search reuses the Text Field size set with Regular default. Built faithfully — no Search-specific size CSS; `.input-xlarge/-large/-regular/-small` inherited. | open (register-only, low) | n/a |
| FND-052 | design-token | low | Popover (node:26345-2762) — panel drop-shadow | The Figma popover card carries a drop-shadow `0px 2px 3px rgba(0,0,0,0.12)` that matches no published elevation token (`--shadow-low` = `0 2px 8px --shadow-color`; different blur + color). Same shape as the tooltip FND-027 departure | A component shadow should resolve to a documented elevation token (hard rule #3) | Publish a shallow popover/tooltip shadow token or confirm the exact value. Built faithfully as the literal `--loop-popover-shadow: 0px 2px 3px rgba(0,0,0,0.12)` in `component-popover.css`. | open (register-only, low) | n/a |
| FND-053 | consistency | low | Popover (node:26345-2762) — native widget has no pointer | The legacy OutSystems `[data-popover]` widget renders only a plain `.popover-bottom` panel with no arrow, but Figma specifies an 18×8 pointer (24px from the leading edge, flipping with placement). Delivered as a CSS `::before` border-triangle keyed to the native `.align-center` / `.align-bottom` classes; left/right placements are not expressible (the legacy widget is below/above + horizontal-align only) | The component's directional pointer should be part of the shipped widget across all documented positions | Confirm the pointer-via-pseudo approach and the below/above-only position set, or migrate to the newer Popover pattern. Built faithfully for top/bottom (up/down) pointers in `loop-popover.css`. | open (register-only, low) | n/a |
| FND-054 | consistency | low | Alert (node:17868-4165) — Action link color, Information multi-line variant | The Action link resolves to `Text/On Light/Link/Primary Enabled` `#004370` (blue-70) for every type and layout **except** the Information **multi-line** variant, which uses `Text/On Light/Link/Secondary Enabled` `#0c0f12` (near-black). So a single semantic element (the Action link) carries two values for one type across layouts (same family as System Alert FND-048 / colour-role consistency FND-008/FND-020) | One canonical value per semantic element/role across layouts | Confirm whether the Information multi-line Action should match the primary link like every other variant, or is an intentional secondary treatment. Built **normalized to primary** `--loop-alert-action: var(--color-text-on-light-link-primary-enabled)` (#004370) for all types/layouts in `component-alert.css`. **2026-07-02: RESOLVED — the updated Alert sticker (node:17868-3944) sets the Information multi-line Action to `#004370` like every other variant; the build already matched. No longer a divergence.** | resolved (design-confirmed) | n/a |
| FND-055 | design-token/fidelity | low | Toast (node:17874-6524) — warning **title** shade `#612705` | The warning toast title uses `lift/text/on state/warning/emphasis` `#612705`, slightly darker than the warning **body** text `#473201` (`Text/On Light/State/Warning`, yellow-90). `#612705` has **no primitive** in the foundation ramp (the orange/brown family stops at yellow-90 `#473201`) — same root pattern as the orphan-semantic findings FND-006/FND-015/FND-033 | Component colours should resolve to a published foundation primitive (hard rule #3); one semantic role = one value | Add a `#612705` "warning emphasis" primitive (or confirm the title may share the body `#473201`). Built faithfully via fallback: `--loop-toast-warning-title: var(--color-text-on-light-state-warning)` (= `#473201`) in `component-toast.css`, so the title renders one shade lighter than Figma's `#612705` pending a primitive. | open (register-only, low) | n/a |
| FND-058 | design-token | low | Layout Side content chrome (node:30132-139328 Header / node:30139-39737 Footer "Form page") | The WBG external **footer surface `#1a1a1a`** has no primitive in the foundation ramp (nearest brand dark is `--color-secondary` #002244 — a different navy hue), and the desktop **grid margin `72px`** ("Grid/Margin/12 Col") has no spacing token | Component colours/spaces should resolve to a published primitive (hard rule #3) | Add a near-black footer primitive (or confirm `#1a1a1a`); publish a 72px grid-margin token (or confirm 48 = `--space-xlarge` is acceptable). Built faithfully via orphan semantic `--color-bg-footer-external: #1a1a1a` (`semantic-colors.css`) + header/footer inline padding on `--space-xlarge` (48px) so both edges align, in `tokens/outsystems-ui-side.css`. | open (register-only, low) | n/a |
| FND-057 | consistency/fidelity | low | Layout Side menu item (node:30132-139314 "Form page"; component `Nav/Menu-Item` node:30154-108685) | The "Form page" frame renders all four side-nav menu items in the **resting** state only — no Active / Hover / Selected variant is exposed for the side-menu item, so the selected-state treatment (pill fill + text/icon colour) is **unspecified** by the design | Every interactive control should specify its hover/active/selected states (so the build doesn't invent brand treatment) | Provide the menu-item hover/active/selected spec. Built **provisionally** with existing Loop role tokens (no invented colours): hover = `--color-neutral-2` (#e7edf3) pill; active/selected = `--color-primary-selected` (blue-10) pill + `--color-primary` (#004370) text/icon, in `tokens/outsystems-ui-side.css`. Resting state matches the frame exactly (white pill, neutral-9 text). | open (register-only, low) | n/a |
| FND-059 | design-token/consistency | low | Typography responsive sizing — `tokens/typography.css` `body.tablet`/`body.phone` (Figma type page node:10995-7259) | The Figma type page exposes **no Desktop/Tablet/Phone breakpoint axis** — only size variants per level. Per the dev request (leverage OutSystems' `body.phone`/`body.tablet`), responsiveness was implemented by stepping the display scale (`--font-size-700…1200` + `huge-72`) down at tablet/phone; body steps (≤600) stay fixed | Per-form-factor type values should be design-specified | Confirm/adjust the tablet & phone step ladder, or publish responsive type variants in Figma. Built faithfully — **desktop unchanged**, every tablet/phone value is an existing on-scale `Font-size/NNN` step (no new sizes invented); only the breakpoint→step assignment is a build proposal. | open (register-only, low) | n/a |
| FND-060 | consistency/fidelity | medium | `Type_Documentation.pdf` (dev-supplied, attached to issue #81) vs Figma type variables (node:10995-7259) | The supplied Type Documentation labels all headings **SemiBold** and shows **H1 36 / H2 32 / H3 28 / H4 20**; the Figma library variables define headings as **Bold (700)** (`Heading/Font Weight`=700), with `Heading/H1/Small`=36, `H2/Small`=32, `H3/Base`=**32** (no H3/Small), and **no H4** at all. So the documentation PDF and the type variables disagree on heading **weight** (SemiBold vs Bold), **H3 size** (28 vs 32), and the **existence of H4** (20) | One canonical type spec; documentation must match the library tokens | Reconcile the Type Documentation with the Figma variables (or vice-versa). **Per stakeholder direction (2026-06-29) headings stay Bold (700)** — matches the Figma variables and the shipped tokens; the PDF's "SemiBold" label + H3=28 / H4=20 are raised for the designer to reconcile. No code change to weight. | open (awaiting designer) | n/a |
| FND-056 | design-token | low | Badge / Label (node:22966-4808) — size scale | The four-size scale (xLarge/Large/Regular/Small) carries off-foundation values: **Regular `13px` & Small `11px`** font-size have no primitive (the type scale is 12/14/16…); **tracking `0.25px`** (Regular/Small) is off the `--letter-spacing-*` scale (same precedent as loop-tag's 0.25px); and **Regular's padding is asymmetric** (left `8` / right `6`) where the other three sizes are symmetric | Component type/space values should resolve to a published primitive (hard rule #3); a single element's padding should be symmetric unless intentional | Add `11px`/`13px` type + a `6px` space primitive (or retarget Regular/Small to 12/14 and symmetric padding), or confirm the literals. Built faithfully as flagged literals in `component-badge-label.css` (per-size `--loop-badge-label-{size}-*` tokens). | open (register-only, low) | n/a |
| FND-061 | consistency | low | Field Wrapper / FieldLabel (node:19336-10226, FieldLabel 15695:5723) — required-marker placement | Figma's FieldLabel places the required marker as a **leading solid red asterisk** (before the label text), whereas the existing Text Field restyle marks required via the native `.mandatory::after { content: " *" }` (a **trailing** asterisk on the label). The new Field Wrapper Block adopts the Figma leading placement (`.loop-field__required`), so two required-marker treatments now exist in the codebase | One canonical required-marker treatment across the form-control family | Confirm the leading-asterisk FieldLabel as the standard and migrate the bare `.loop-field` required marker to match (or keep `.mandatory::after` for plain native-label use). Built faithfully to Figma: leading `.loop-field__required` in the Block; legacy trailing `.mandatory::after` retained for the bare restyle. | open (register-only, low) | n/a |
| FND-063 | consistency/fidelity | low | Dropdown / Select (node:18787-4817) — popup option-row horizontal padding | Frozen-ref re-audit (2026-07-04, `loop/refs/cmp-dropdown-select/`): the Figma popup item padding is **12px** (`Spacing/xsmall`, spec.md) but the build uses **16px** `var(--loop-select-padding-inline)` on both popup surfaces (`src/blocks/loop-dropdown.css:126` native rows, `:238` VirtualSelect rows) — likely a deliberate align-with-field-text choice, but undisclosed | Code should match the ref value, or the intentional divergence must be disclosed | Either retarget both paddings to `var(--space-xsmall, 12px)` or confirm the 16px alignment treatment. Flagged by the ref-based checker pilot; awaiting call. | open (register-only, low) | n/a |
| FND-064 | fidelity | low | Dropdown / Select (node:18787-4817) — native popup row text size | Frozen-ref re-audit (2026-07-04): the ref popup item text is **14px** (`Body/Text/Small/Regular`, `Font-size/200`) but the native-pattern rows use **13px** `var(--loop-select-text-size)` (`src/blocks/loop-dropdown.css:128`), while the artifact's own VirtualSelect rows correctly use `var(--font-size-200, 14px)` (`:240`) — the two popup surfaces are internally inconsistent. (FND-031 covers the closed field's 13px, not the popup rows.) | Popup rows should match the ref (14px) and be consistent across surfaces | Change `:128` to `var(--font-size-200, 14px)` (and revisit the 40px row min-height at `:124` vs the ref's ~37px item in the same pass). Flagged by the ref-based checker pilot. | open (register-only, low) | n/a |
| FND-065 | design-token | low | Shadow color drift follow-up to FND-003 (node:19737-9489 / Select ref node:18787-4817 / Card ref node:20315-6129) | The 2026-06-24 sign-off fixed the canonical shadow color as `#21262d29` (`tokens/shadows.css:3`), with the Figma side to be reconciled — but the 2026-07-04 frozen ref still emits `effects/shadow/default` = **`#00396b29`**, so Figma has not been reconciled (or drifted again). Code is correct per the sign-off; the delta now shows up as a ref-vs-code diff on every shadowed component | One canonical shadow color in both Figma and tokens | Reconcile the Figma `effects/shadow/default` variable to `#21262d29` per the FND-003 ruling (designer action). **No code change.** | open (awaiting designer) | n/a |
| FND-062 | consistency | low | Alert (node:17868-3944, 2026-07-02 update) — warning **message** shade differs by layout | The updated Alert sticker colours the warning message `Text/On State/Warning/Default` `#896001` (yellow-base) in the **single-line** layout but `Text/On Light/State/Warning` `#473201` (yellow-90) in the **multi-line** layout, while error/information/success each use one message colour across both layouts. So a single semantic element (the warning message) carries two values across layouts (same family as System Alert FND-048 / Alert FND-054) | One canonical value per semantic element/role across layouts | Reconcile the warning message to one shade. Built **faithfully per-layout** (per stakeholder direction 2026-07-02): `--loop-alert-warning-message` (#896001, single) + `--loop-alert-warning-message-multi` (#473201, multi) in `component-alert.css` / `loop-alert.js`. | open (register-only, low) | n/a |
| FND-066 | consistency | low | Alert (node:17868-3944 / success variant 17868-4020) — success icon is the **info "i" glyph**, not a check | Both success layouts of the Alert sticker draw a filled circle containing the info "i" (exported vector is structurally identical to FA solid `circle-info`), while success elsewhere in the library uses a check mark (Toast 17874-6524 circle-check ring, Badge Status check) — so the success state carries the information glyph in the Alert family only | One canonical glyph per semantic state across the library (success conventionally = check) | Confirm the info-glyph success Alert is intentional, or update the sticker to a circle-check. Built **faithfully** per the sticker: `loop-alert.js` success renders FA solid `circle-info` (`\f05a`); preview native-Alert/FeedbackMessage success mocks aligned to match. | open (register-only, low) | n/a |
| FND-067 | a11y/contrast | high | System Alert (node:17873-7408) — `warning` type **title**, both layouts | The warning title fill is white-75 `#ffffffbf` over the amber bg `#e19d00` ≈ **1.90:1** (16px/Bold is not WCAG large text → 4.5:1 required; fails the 3:1 floor too), while the sticker's own sibling message/action/icon use dark `#473201` ≈ 5.2:1 — the title reads nearly invisible, and white-75 looks like a leftover from the dark-bg types' shared title role. Confirmed in the rendered sticker, not just dev-mode fills | WCAG 2.2 SC 1.4.3 (text contrast) | Set the warning title to `Text/On Light/State/Warning` `#473201` like the banner's other elements. Built faithfully — `--loop-sysalert-warning-title: var(--color-gray-alpha-white-75)` per the sticker (stakeholder-confirmed 2026-07-04); disclosed only (hard rule #4). Filed as GitHub Bug per the high-sev gate; cf. FND-048. | open (awaiting designer) | [#141](https://github.com/kharloridado/wbg/issues/141) |
| FND-068 | consistency | low | Text Area responsive (node:19336-10332) — phone text size 12px | The phone breakpoint specifies `loop/text area/font size` = 12px (line-height 16). Mobile Safari auto-zooms the viewport when focusing any form control whose font-size is below 16px, so on iPhone the page will jump-zoom on tap into the textarea; the tablet/desktop 14px steps sit below the threshold too, but iPadOS/desktop don't auto-zoom | Mobile UX convention — iOS Safari focus-zoom threshold is 16px (not a WCAG criterion; text stays resizable) | Consider ≥16px input text on phone, or accept the zoom behaviour explicitly. Built faithfully: `.phone .form-control[data-textarea]` steps to 12px/16px per the frozen ref (`loop/refs/cmp-text-area`). Register-only per the low gate | open | n/a |
| FND-069 | consistency | low | Search Field (node:17191-8819 / Search Field Container 17191:6079) — label→input gap 4px vs family 6px | The Search Field composes its FieldLabel **4px** above the input (`loop/label/gap` resolves 4 in the Search container) while the Text Field (19336-9729), Checkbox group (19336-17818) and Toggle group (25862-14729) all use **6px** for the same label→control gap — one wrapper role, two values | One canonical FieldLabel→control gap across the form-control family | Reconcile the Search Field to the family 6px (or confirm 4px as intentional). Built faithfully: `.loop-field:has(.osui-search)` re-points `--loop-field-label-gap` to 4px (`loop-search.css`); one-line revert when design reconciles. Register-only per the low gate. | open (register-only, low) | n/a |
