# Brand Guidelines — <Customer>

The source of truth for brand conformance. Findings reference this. Fill it in at kickoff.

## Color palette (the brand colors — build to these exactly)
| Token | Hex | Usage |
|---|---|---|
| --color-primary | `#______` | |
| --color-secondary | `#______` | |
| ... | | |

> Any color in the design not listed here → `brand-color` finding. Don't add to the palette without the brand owner's sign-off.

## Typography
- Font family: `______`
- Weights in use: `______`
- Type scale: see `tokens/typography.css`

## Spacing
- Base: 4pt. Scale: see `tokens/spacing.css`.

## Logo / iconography / elevation
`<rules, clear-space, shadow scale, etc.>`

## Known signed-off exceptions
`<deviations the brand owner has already approved — so they aren't re-flagged>`
